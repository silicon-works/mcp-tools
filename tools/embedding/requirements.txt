# BGE-M3 Embedding MCP Server Dependencies
# Doc 22 §Part 5

# PyTorch (CPU-only for smaller image, GPU can be added if needed)
torch==2.2.0
--extra-index-url https://download.pytorch.org/whl/cpu

# Transformers and FlagEmbedding for BGE-M3
transformers==4.38.0
FlagEmbedding==1.2.10

# Numerical operations
numpy==1.26.0

# MCP server framework
mcp>=1.0.0
